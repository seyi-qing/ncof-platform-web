'use client'
import {useEffect,useState} from 'react';import {Plus,FileText,ExternalLink} from 'lucide-react';import AppShell from '@/components/AppShell';import {api} from '@/lib/api';import {getRole} from '@/lib/auth';import {Card,PageHeader,Table,Button,Modal,Field,Textarea,ErrorBox,SuccessBox,Empty} from '@/components/UI';
export default function Documents(){const[data,setData]=useState<any[]>([]);const[open,setOpen]=useState(false);const[err,setErr]=useState('');const[success,setSuccess]=useState('');const[form,setForm]=useState({title:'',document_type:'policy',storage_url:'',description:''});const role=getRole();const load=()=>api<any[]>('/governance/documents').then(setData).catch(e=>setErr(e.message));useEffect(()=>{void load()}, []);async function save(e:any){e.preventDefault();try{await api('/governance/documents',{method:'POST',body:JSON.stringify({...form,description:form.description||null})});setSuccess('Document added.');setOpen(false);load()}catch(e:any){setErr(e.message)}}return <AppShell title="Documents"><div className="content"><PageHeader title="Documents" description="Association policies, minutes, records and shared resources." action={role!=='member'&&<Button onClick={()=>setOpen(true)}><Plus size={15}/>Add document</Button>}/>{err&&<ErrorBox message={err}/>} {success&&<SuccessBox message={success}/>}<Card><div className="section-title"><h3><FileText size={17}/> Document library</h3></div>{data.length?<Table headers={['Title','Type','Description','Added','Open']} rows={data.map(d=>[d.title,d.document_type,d.description||'\u2014',new Date(d.created_at).toLocaleDateString(),<a className="btn secondary" href={d.storage_url} target="_blank" rel="noreferrer"><ExternalLink size={13}/>Open</a>])}/>:<Empty text="No documents available."/>}</Card></div>{open&&<Modal title="Add document" onClose={()=>setOpen(false)}><form onSubmit={save}><Field label="Title" value={form.title} onChange={(e:any)=>setForm({...form,title:e.target.value})} required/><Field label="Document type" value={form.document_type} onChange={(e:any)=>setForm({...form,document_type:e.target.value})} required/><Field label="Storage URL" value={form.storage_url} onChange={(e:any)=>setForm({...form,storage_url:e.target.value})} required/><Textarea label="Description" value={form.description} onChange={(e:any)=>setForm({...form,description:e.target.value})}/><div className="form-actions"><Button variant="ghost" type="button" onClick={()=>setOpen(false)}>Cancel</Button><Button>Add document</Button></div></form></Modal>}</AppShell>}
